model_config_list {
 config {
    name: 'potatoes_model'
    # This points to the folder INSIDE the container
    base_path: '/models/saved_models' 
    model_platform: 'tensorflow'
    model_version_policy: {all: {}}
        }
}